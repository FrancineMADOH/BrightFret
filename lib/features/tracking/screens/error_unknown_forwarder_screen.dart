import 'package:flutter/material.dart';

/// Stub for ErrorUnknownForwarderScreen — S15.
/// Full implementation in F1.8: shown when tracking code prefix is not in forwarders.json.
class ErrorUnknownForwarderScreen extends StatelessWidget {
  const ErrorUnknownForwarderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ErrorUnknownForwarderScreen — S15')),
    );
  }
}
