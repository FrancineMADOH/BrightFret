import 'package:flutter/material.dart';

/// Stub for MyShipmentsScreen — S12.
/// Full implementation in F1.9: list of saved shipments from Hive with status refresh.
class MyShipmentsScreen extends StatelessWidget {
  const MyShipmentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('MyShipmentsScreen — S12')),
    );
  }
}
